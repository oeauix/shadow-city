# 🔌 تحلیل Unity MCP + ☁️ یونیتی ابری رایگان — راهنمای کامل

---

# بخش ۱: Unity MCP چیست و چطور کار می‌کند

**رپو:** `github.com/CoplayDev/unity-mcp` — ۱۲.۹هزار ستاره، MIT (رایگان تجاری)، نسخه v10.1.0
ساخته Justin Barnett، بعداً به Coplay فروخته شد و همان‌جا توسعه ادامه دارد.

## معماری (از بررسی عمیق سورس و کامیت‌ها)
```
کلاینت AI (Claude/Cursor/...)
   │  پروتکل MCP (stdio یا HTTP JSON-RPC 2.0)
   ▼
سرور پایتونی MCP  ← با uvx از خود رپو اجرا می‌شود (mcpforunityserver)
   │  WebSocket (برای سرور remote) یا پورت محلی 6400
   ▼
پکیج C# داخل Unity Editor (Bridge)
   │  McpToolRegistry
   ▼
ابزارها: manage_gameobject, manage_asset, manage_scene,
         ساخت/ویرایش اسکریپت + اعتبارسنجی، خواندن Console,
         batch_execute، اجرای منو، حتی custom tool با attribute
```

## نکات به‌دردبخور که از کامیت‌ها درآوردم
1. **ترنسپورت پیش‌فرض الان HTTP است** (localhost:8080) نه stdio — یعنی سرور یک REST/JSON-RPC endpoint واقعی دارد.
2. **WebSocket hub اضافه شده مخصوصاً برای اینکه «سرور MCP ریموت» بتواند به Unity محلی وصل شود** — یعنی سناریوی «AI یک‌جا، Unity جای دیگر» رسماً پشتیبانی می‌شود.
3. نصب پکیج Unity: `Window > Package Manager > + > Add package from git URL`:
   `https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#main`
4. سرور جدا نصب نمی‌خواهد — دکمه «Start Local HTTP Server» داخل پنجره MCP خود یونیتی هست (uvx خودش می‌کشد). فقط باید **uv/uvx** و پایتون روی سیستم باشد.
5. ابزار `manage_script` اعتبارسنجی کامپایل هم دارد؛ `read_console` خطاها را برمی‌گرداند — یعنی حلقه «کد بزن، خطا بخوان، اصلاح کن» کامل بسته می‌شود.
6. یک ابزار Roslyn هم دارند برای کامپایل runtime در Play mode.
7. Unity 6.3+ حتی MCP رسمی خود یونیتی را دارد (Project Settings > AI > Unity MCP) — ولی برای 2022.3 ما همین CoplayDev درست است.

## من (این Agent) چطور می‌توانم وصل شوم؟
صادقانه: **کلاینت MCP قابل‌افزودن ندارم** — ابزارهای من ثابت‌اند. ولی یک راه واقعی هست:
سرور unity-mcp روی HTTP با **JSON-RPC 2.0** حرف می‌زند و من `bash + curl` دارم! پس:

```
Unity تو (لپ‌تاپ یا Codespace)
  → MCP for Unity window → Start Local HTTP Server (پورت 8080)
  → یک تانل عمومی: ngrok / cloudflared / localtunnel روی پورت 8080
  → آدرس https عمومی را به من بده
  → من با curl مستقیم JSON-RPC می‌زنم: initialize → tools/list → tools/call
```
یعنی می‌توانم GameObject بسازم، اسکریپت ویرایش کنم و **Console یونیتی تو را از همین‌جا بخوانم و باگ‌ها را بدون رفت‌وبرگشت اسکرین‌شات رفع کنم.** محدودیت: تانل باید تا وقتی کار می‌کنم باز بماند و هر پیام تو یک نوبت کاری من است (نه نظارت دائم زنده).

⚠️ امنیت: تانل عمومی یعنی هرکس URL را بداند به ادیتورت فرمان می‌دهد — فقط حین جلسه کاری باز نگه دار، بعد ببند.

---

# بخش ۲: یونیتی ابری رایگان — تحلیل عمیق گزینه‌ها

## خلاصه حکم برای وضعیت تو (سیستم ضعیف، بدون کردیت‌کارت):

| گزینه | چی می‌دهد | رایگان؟ | کارت می‌خواهد؟ | حکم |
|---|---|---|---|---|
| 🥇 **GameCI روی GitHub Actions** | بیلد کامل APK + WebGL در ابر | **بی‌نهایت روی repo عمومی**؛ ۲۰۰۰ دقیقه/ماه روی خصوصی | ❌ نه | **راه اصلی ما** |
| 🥈 **GitHub Codespaces + ادیتور Unity + noVNC** | خود ادیتور GUI توی مرورگر | ۱۲۰ core-hour/ماه (~۳۰ ساعت روی ۴ هسته) + ۱۵GB | ❌ نه | برای وقتی واقعاً ادیتور لازم است |
| Unity Build Automation (رسمی) | بیلد ابری رسمی | فری‌تیر دارد (بدون کارت شروع می‌شود) ولی سقف که رد شد **بلاک فوری تا کارت بدهی** + ریسک حذف دیتا | شروع نه، ادامه بله | ❌ برای ما مناسب نیست |
| Gitpod و مشابه | محیط ابری | ~۵۰ ساعت/ماه | ❌ | جایگزین Codespaces، مزیتی ندارد |

## 💡 بینش کلیدی که همه‌چیز را عوض می‌کند
پروژه ما **عمداً runtime-generated** طراحی شده: صحنه فقط یک Bootstrap دارد و کل شهر/UI/موجودات را کد می‌سازد. یعنی برخلاف پروژه‌های معمول یونیتی، **ما تقریباً به ادیتور نیاز نداریم!** ادیتور فقط برای بیلد لازم بود — و بیلد را ابر انجام می‌دهد. گردش کار جدید:

```
من کد را می‌نویسم/عوض می‌کنم → به GitHub push می‌شود
→ GameCI خودکار می‌سازد: ① بیلد WebGL (توی مرورگر گوشی/PC ضعیفت باز کن، ۱ دقیقه)
                          ② فایل APK (روی گوشیت نصب کن)
→ تو فقط بازی می‌کنی و بازخورد می‌دهی
```
**دیگر لازم نیست هیچ‌وقت روی سیستم ضعیفت یونیتی باز کنی** (مگر برای کارهای بصری خاص، که آن هم Codespaces هست).

## نقشه اجرا — قدم‌به‌قدم (فایل‌هایش را ساخته‌ام، داخل زیپ‌اند)

### مرحله A — یک‌بار برای همیشه: ریپو + لایسنس (~۲۰ دقیقه، فقط مرورگر)
1. حساب GitHub بساز (اگر نداری) → New repository → **Public** (تا Actions بی‌نهایت رایگان باشد) → اسم: `shadow-city`
2. محتویات پوشه `unity/ShadowCity` را آپلود کن (Upload files در وب، یا git push)
   — فایل‌های `.github/workflows/` که ساخته‌ام هم داخلش هستند
3. تب **Actions** → workflow «**Acquire activation file**» → Run workflow
   → بعد از اتمام، artifact را دانلود کن → فایل `Unity_v2022.3.22f1.alf` را دربیاور
4. برو به **license.unity3d.com/manual** → فایل .alf را آپلود کن → گزینه **Personal / free** → فایل `.ulf` دانلود می‌شود
   (این سایت فقط لاگین Unity ID می‌خواهد — همان حسابی که برای Hub ساختی؛ کارت نمی‌خواهد)
5. در ریپو: Settings → Secrets and variables → Actions → سه Secret بساز:
   - `UNITY_LICENSE` = کل متن فایل .ulf (با Notepad باز کن، همه را کپی کن)
   - `UNITY_EMAIL` = ایمیل حساب یونیتی
   - `UNITY_PASSWORD` = رمز حساب یونیتی
   (GameCI متن‌باز است و اعلام کرده ایمیل/رمز را ذخیره نمی‌کند — فقط برای فعال‌سازی حین بیلد)

### مرحله B — بیلد خودکار (از این به بعد خودکار)
- هر push → workflow «Build ShadowCity» اجرا می‌شود:
  - **WebGL** → خودکار روی **GitHub Pages** منتشر می‌شود → لینک بازی: `https://<username>.github.io/shadow-city/`
  - **Android APK** → در Artifacts همان run → دانلود و نصب روی گوشی
- بیلد اول کند است (~۳۰-۵۰ دقیقه چون کش خالی است)، بعدی‌ها ~۱۰-۱۵ دقیقه.

### مرحله C (اختیاری) — ادیتور کامل توی مرورگر با Codespaces
فایل `.devcontainer/` هم ساخته‌ام: روی ریپو دکمه **Code → Codespaces → Create** بزنی، ماشین ابری بالا می‌آید، اسکریپت داخلش ادیتور Unity + دسکتاپ مجازی (noVNC) نصب می‌کند و از تب مرورگر به ادیتور کامل می‌رسی. واقع‌بینی: روی ماشین ۴-هسته رایگان، ادیتور کار می‌کند ولی روان نیست — برای کارهای گرافیکی گاه‌به‌گاه خوب است، نه کار روزمره. سهمیه: ۱۲۰ core-hour/ماه یعنی ~۳۰ ساعت (یادت باشد Codespace را Stop کنی وقتی تمام شد).

### تقسیم کار جدید من/تو
- 🤖 من: کد، ایست، فایل‌های workflow، رفع باگ از روی لاگ Actions (لاگ‌ها متن‌اند، مستقیم می‌خوانمشان اگر ریپو عمومی باشد!)
- 🧍 تو: ۵ قدم مرحله A (یک‌بار)، بعدش فقط APK نصب کن / لینک وب را باز کن، بازی کن، بازخورد بده

### درباره لایسنس شخصی و قانونی بودن
Unity Personal رایگان است و برای درآمد زیر ۲۰۰هزار دلار/سال مجاز؛ GameCI هم روش استاندارد جامعه یونیتی برای CI است و همین فلوی activation رسماً در مستنداتش است. نکته: فایل .ulf را جایی عمومی نگذار — فقط در Secrets.
