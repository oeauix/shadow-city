# 🎮 SHADOW CITY — Unity Project / پروژه یونیتی
## راهنمای ایمپورت قدم‌به‌قدم (Unity 2022.3.22f1)

### قدم ۱ — ساخت پروژه
1. Unity Hub → **New Project** → قالب **3D (URP)** را انتخاب کن
   ⚠️ **اگر گزینه «3D (URP)» نداری** (فقط 2D/3D/2D URP داری): اشکالی ندارد!
   قالب **3D** معمولی را انتخاب کن — در قدم ۴.۵ با دو کلیک URP را فعال می‌کنیم.
2. اسم: `ShadowCity` → Create
3. صبر کن پروژه باز شه

### قدم ۲ — کپی فایل‌ها
پوشه `unity/ShadowCity/Assets/ShadowCity` از این بسته را کپی کن داخل پوشه `Assets` پروژه‌ات:
```
YourProject/Assets/ShadowCity/   ← کل این پوشه
  Scripts/  (18 فایل C#)
  Resources/Audio/  (23 فایل صوتی ElevenLabs)
  Fonts/Vazirmatn-Regular.ttf
```
بعد از کپی، یونیتی خودش کامپایل می‌کند. **نباید هیچ خطای قرمزی در Console باشد** (این کد با ۴۳ تست منطقی و کامپایل کامل تأیید شده).

### قدم ۳ — فونت فارسی (۳۰ ثانیه)
1. در Project window برو به `ShadowCity/Fonts/Vazirmatn-Regular`
2. آن را بکش داخل پوشه `ShadowCity/Resources/` (باید داخل Resources باشد تا در رانتایم لود شود)

### قدم ۳.۵ — مدل‌های AI (اختیاری ولی توصیه‌شده، ~۲ دقیقه)
۱۳ مدل GLB آماده در `ShadowCity/Models/` هست (کاراکترها، ۵ ماشین، پراپ‌ها — همه رایگان تولید شده).
برای فعال‌سازی:
1. **Window → Package Manager → + → Add package by name** → بنویس: `com.unity.cloud.gltfast` → Add
   (بعد از نصب، GLB ها خودکار import می‌شوند و preview پیدا می‌کنند)
2. پوشه بساز: `ShadowCity/Resources/Models/`
3. هر ۱۳ فایل GLB را از `ShadowCity/Models/` **بکش داخل** `ShadowCity/Resources/Models/`
4. تمام! بازی خودش تشخیص می‌دهد: ماشین‌ها و کاراکترها به‌جای مکعب، مدل واقعی می‌شوند
   (اگر این مرحله را رد کنی بازی با مدل‌های procedural اجرا می‌شود — هیچ خطایی نمی‌گیری)

### قدم ۴.۵ — فعال‌سازی URP (فقط اگر پروژه را با قالب 3D معمولی ساختی)
اگر پروژه‌ات URP نیست، دو کلیک:
1. منو → **Shadow City → 1. Install URP Package** → صبر کن نوار پیشرفت پایین-راست تمام شود (~۱-۲ دقیقه)
2. منو → **Shadow City → 2. Activate URP Pipeline** → پیام تأیید می‌گیری ✔
(اگر پروژه از اول URP بود، این قدم را کلاً رد کن)

### قدم ۵ — ساخت صحنه (یک کلیک)
منوی بالای یونیتی → **Shadow City → Setup Scene (run once)**
(صحنه Main ساخته و ذخیره می‌شود و به Build Settings اضافه می‌شود)

### قدم ۶ — بازی کن! ▶
دکمه Play را بزن. باید ببینی:
- منوی اصلی با دوربین سینمایی چرخان روی شهر
- دکمه‌ها: بازی جدید / ادامه / زبان / کیفیت
- **New Game** → وارد شهر می‌شوی، گرگ‌ومیش نئونی
- WASD حرکت · موس دوربین · Shift دویدن · Space پرش
- **E** سوار شدن ماشین · **Q پالس پژواک** · Esc توقف
- دکمه زبان → کل UI فارسی RTL می‌شود (متن‌ها با حروف چسبیده درست)

### قدم ۷ — بازخورد بده
این‌ها را برایم بفرست:
1. اسکرین‌شات از منو و داخل بازی (روز و شب)
2. هر پیام قرمز/زرد Console
3. FPS تقریبی (Stats بالای Game view)
4. حس کنترل: سرعت راه رفتن؟ دوربین؟ ماشین؟

---
## ابزارهای داخل ادیتور
| منو | کار |
|---|---|
| Shadow City → Setup Scene | ساخت صحنه اصلی |
| Shadow City → Tripo Importer | تولید مدل AI (وقتی اعتبار شارژ شد) — پک ۱۴ پرامپت آماده دارد |
| Shadow City → Configure Android Build | تنظیمات اندروید (IL2CPP، ARM64) |

## چه چیزهایی الان داخل بازی است (فاز ۱)
✅ شهر ۵ محله‌ای تولیدشونده با پنجره‌های نورانی (رزونانس→رنگ نئون)
✅ چرخه روز/شب با غروب طلایی + چراغ‌های خیابان خودکار
✅ راه رفتن/دویدن/پرش + دوربین سوم‌شخص GTA با برخورد
✅ ماشین‌ها: سرقت، رانندگی آرکید، ترافیک هوشمند، چراغ ترمز/جلو
✅ عابران با فرار/سلام بر اساس رزونانس
✅ پلیس ۵ ستاره با مدل «آخرین موقعیت دیده‌شده» (فرار منصفانه)
✅ پالس پژواک (Q) — حلقه نور + آشکارسازی ماشین‌ها و ماموریت‌ها
✅ RPG: ۶ مهارت روز/شب، XP با پاداش ساعت دوگانه
✅ اقتصاد + فروشگاه با قیمت رزونانسی
✅ ۷ نوع ماموریت (تاکسی/تحویل/پیک/مسابقه/سرقت/قاچاق + داستان S1)
✅ صداهای ElevenLabs (شلیک، انفجار، آژیر، باران، پالس...) + دیالوگ EN/FA
✅ موسیقی جنریتیو (پد روز / آرپ شب / دیتون ترس) — بدون فایل!
✅ ذخیره/لود + autosave هر ۳ دقیقه
✅ کنترل لمسی کامل موبایل (جوی‌استیک + دکمه‌ها) — خودکار فعال می‌شود
✅ سه سطح کیفیت گرافیک

## فاز ۲ (بعد از بازخورد تو)
- مدل‌های Tripo (وقتی شارژ شد؛ ~۱۰-۱۵ دلار برای ست کامل)
- آب‌وهوا + باران، مینی‌مپ، گنگ‌ها، رویدادهای خیابانی، شاردها
- AdMob/تپسل + بیلد بازار/مایکت
